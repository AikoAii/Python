#To append elements from another list to the current list, use the extend() method.

#Add the elements of tropical to thislist:
listname = ["asep", "beni", "citra"]
age = [16, 18, 15]
listname.extend(age)
print(listname)
#The elements will be added to the end of the list.