#To create a tuple with only one item, you have to add a comma after the item, otherwise Python will not recognize it as a tuple.
top = ("apple",) #One item tuple, remember the comma:
print(type(top)) #tuplen
flse = ("not comma")
print(type(flse)) #str